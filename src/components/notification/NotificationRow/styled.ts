import { styled } from '@mui/material';

export const DotDiv = styled('div')({
  position: 'absolute',
  top: 'calc(50% - 2px)',
  left: '16px',
  width: '5px',
  height: '5px',
  borderRadius: '50%',
  background: '#0276E3',
});
